# Installation & Running

Using **NodeJS v16** is required at the moment.

To run project:

1. Download the codebase
2. Navigate to the folder
3. `npm i`
4. `npm start`

To run application on the web for testing in monday.com use `ngrok http http://localhost:3000`.
